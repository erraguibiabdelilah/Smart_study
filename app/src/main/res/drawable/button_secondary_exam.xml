<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Couleur du bouton -->
    <solid android:color="#FFFFFF"/>

    <!-- Coins arrondis -->
    <corners android:radius="30dp"/>

    <!-- Bordure -->
    <stroke
        android:width="3dp"
        android:color="#00093C"/>

    <!-- Padding interne -->
    <padding
        android:left="16dp"
        android:top="10dp"
        android:right="16dp"
        android:bottom="10dp"/>
</shape>
