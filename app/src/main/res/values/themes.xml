<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Smart_Study" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/primary_dark</item>
        <item name="colorOnPrimaryContainer">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent_blue</item>
        <item name="colorOnSecondary">@color/primary_dark</item>
        <item name="colorSecondaryContainer">@color/background_primary</item>
        <item name="colorOnSecondaryContainer">@color/text_primary</item>

        <!-- Background color. -->
        <item name="android:colorBackground">@color/background_primary</item>

        <!-- Surface color (for cards, dialogs, etc.). -->
        <item name="colorSurface">@color/background_card</item>
        <item name="colorOnSurface">@color/text_primary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <style name="Theme.Smart_Study" parent="Base.Theme.Smart_Study" />

    <!-- Custom Button Styles -->
    <style name="Theme.Smart_Study.Button.Success" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/success_green</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Smart_Study.Button.Error" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/red_error</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>